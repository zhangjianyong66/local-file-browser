# 实现计划

1. 读取前端和后端规范，确认现有 preview API 与 Goldmark 初始化位置。
2. 启用 Goldmark 安全扩展（至少表格），补充/调整 Markdown 预览测试。
3. 新增独立 Markdown 阅读页及样式，展示元信息、下载、加载/错误/超限状态和渲染内容。
4. 修改目录文件点击行为，仅对 `.md` 打开新窗口，保留其他文件详情面板行为。
5. 运行 `node --check`、`go test -race ./...`、`go vet ./...`、`go build ./...`，并通过本地服务验证新窗口页面与表格渲染。
